# docs/

Place the working note PDF here
(*Delphes Sample Preparation and a Sustainable Fast-Simulation Analysis Pipeline
for NSBI Phenomenology Studies*, J. Pan) and per-campaign provenance logs.

The design implemented by this repo is summarised in the top-level
[`DESIGN.md`](../DESIGN.md). Per-run provenance is emitted automatically into
each `outputs/.../report.json` (see `core/provenance.py`).
