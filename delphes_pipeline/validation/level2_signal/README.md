# Level 2 — Signal acceptance × efficiency (STUB)

Per-channel A×ε for SM `HH → bb̄ τ⁺τ⁻`, compared against values backed out of
published CMS/ATLAS Run-2 `bb̄τ⁺τ⁻` yields; target agreement 20–30% (note §6.1).

Blocked on: the resolved-category selection (res1b/res2b + the elliptical
`(m_ττ, m_bb)` mass cut, note Eq. 3) running on the flat ntuple. Expose
`run(ctx) -> list[CheckResult]` reading `ctx.ntuple`.
