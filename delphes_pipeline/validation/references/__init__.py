"""Reference curves: the card's transcribed formulas + digitised-POG drop-ins.

``card_formulas.expected(quantity, pt, eta)`` is injected into the core
``ReferenceStore`` by the orchestrator; digitised POG JSONs live in ``data/``.
"""
